services:
  db:
    image: mysql
    container_name: mysql_bookly
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3308:3306"
    volumes:
      - bookly_data:/var/lib/mysql

volumes:
  bookly_data: